let myFont;
function preload() {
  myFont = loadFont('assets/Nexa.otf');
}

function setup() {
  // fill('#ED225D');
  // textFont(myFont);
  // textSize(36);
  // text('p5*js', 10, 50);
  createCanvas(windowWidth, windowHeight);
}

function draw() {
  background(1000);
  let anchorGrey = color(237);
  background(anchorGrey);
}